#include <iostream>
using namespace std;

int main()
{
   /*1)
    int num;
    cout << "Input number: ";
    cin >> num;

    if (num % 2 != 0)
        cout << "Number " << num << " Odd!" << endl;
    else
        cout << "Number " << num << " Even!" << endl;
    system("pause");
    return 0; */

    /*2)
    int num1, num2;

    cout << "Input number 1:__\b\b";
    cin >> num1;
    cout << "Input number 2:__\b\b";
    cin >> num2;
    if (num1 != num2)
    {
        if (num1 < num2)
            //cout << "Number 2 bigger than Number 1!" << endl;
            //cout << "Number : " << num2 << " bigger then Number : " << num1 << endl;
            cout << "Number :__\b\b" << num2 << endl;
        else
            //cout << "Number : " << num1 << " bigger then Number : " << num2 << endl;
            cout << "Number :__\b\b" << num1 << endl;
    }
    else
        cout << "Numberst are equal!" << endl;

    system("pause");
    return 0; */
    /*3)

        int val;
        cin >> val;
        if (val == 0)
            cout << "Your number is 0\n";
        else if (val < 0)
            cout << "Your number is negative\n";
        else
            cout << "Your number is positive\n";

        return 0;
    */
    /*4)
    int a, b;
    cout << "Input a: " << endl;
    cin >> a;
    cout << "Input b: " << endl;
    cin >> b;
    if (a == b)
        cout << "Numbers are equal" << endl;
    else if (a > b)
        cout << a << "," << b << endl;
    else if (a < b)
        cout << b << "," << a << endl;
    else
        cout << "ERROR" << endl;
    return 0; */

}